LANCTL portable
===============

Run LANCTL.exe from this directory. Mutable data is stored in data/lanctl beside
the executable. The portable package does not modify PATH, ProgramData, the
registry, services, firewall rules, or SSH/HTTPS settings. Preserve data/lanctl
when replacing the executable during an update.
